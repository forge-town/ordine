{
  "name": "@ordine/create",
  "version": "0.0.2-preview.10",
  "private": false,
  "description": "Create a local Ordine instance — npm create @ordine",
  "repository": {
    "type": "git",
    "url": "https://github.com/forge-town/ordine",
    "directory": "apps/create"
  },
  "bin": {
    "create-ordine": "dist/index.js"
  },
  "files": [
    "dist",
    "app",
    "migrations"
  ],
  "type": "module",
  "exports": {
    "./migrations": "./src/migrations.ts"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build:cli": "bun build ./src/index.ts --target=node --format=esm --outfile=./dist/index.js",
    "build:app": "bun ./src/copyAppBuild.ts",
    "build": "bun run build:cli && bun run build:app",
    "check-types": "tsc --noEmit",
    "lint": "oxlint --config ./.oxlintrc.json src/",
    "quality": "bun run check-types && bun run lint && bun run test",
    "test": "vitest run",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "neverthrow": "^8.2.0",
    "postgres": "^3.4.8",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@ordine/app": "workspace:*",
    "@repo/oxlint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^25.6.0",
    "typescript": "5.9.2",
    "vitest": "^4.1.4"
  },
  "engines": {
    "node": ">=20"
  }
}
