{
  "name": "@ordine/cli",
  "version": "0.0.2-preview.10",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/forge-town/ordine",
    "directory": "apps/cli"
  },
  "bin": {
    "ordine": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "bun": "./src/index.ts",
      "default": "./dist/index.js"
    },
    "./mcp-manager": {
      "types": "./src/mcp/manager.ts",
      "bun": "./src/mcp/manager.ts",
      "default": "./dist/manager.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun build ./src/index.ts ./src/mcp/manager.ts --target=node --format=esm --outdir=./dist --entry-naming=[name].js",
    "check-types": "tsc --noEmit",
    "lint": "oxlint --config ./.oxlintrc.json src/",
    "quality": "bun run check-types && bun run lint && bun run test",
    "test": "vitest run",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "commander": "^14.0.0",
    "neverthrow": "^8.2.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@repo/oxlint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^25.6.0",
    "typescript": "5.9.2",
    "vitest": "^4.1.4"
  },
  "engines": {
    "node": ">=20"
  }
}
