{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:allow-kill",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "args": [
            {
              "validator": "regex",
              "value": "^.*/resources/server/server-bundle\\.mjs$"
            }
          ],
          "name": "binaries/ordine-server",
          "sidecar": true
        }
      ]
    }
  ]
}
