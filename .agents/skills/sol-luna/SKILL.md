---
name: sol-luna
description: Compatibility alias for the Ordine Sol-Terra workflow. Use when the user invokes the historical $sol-luna name; execution is routed to Terra because the current Codex runtime cannot spawn Luna from Sol.
---

# Sol-Luna Compatibility Alias

Apply the complete `$sol-terra` workflow from `../sol-terra/SKILL.md`.

Use `terra_explorer`, `terra_implementer`, and `terra_tester`. Do not spawn the legacy `luna_*` roles or claim that Luna executed the task. Preserve the same delegation contract, acceptance gate, and Sol High escalation gate defined by `$sol-terra`.
